{
  "name": "schemas",
  "version": "1.0.0",
  "description": "Invoice event schema generation",
  "main": "index.js",
  "scripts": {
    "generate-json": "typescript-json-schema -o=dist/schema.json 'src/*.ts' '*'",
    "generate-csharp": "quicktype --src=dist/schema.json --lang=csharp --out=dist/schema.cs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "quicktype": "^15.0.260",
    "typescript": "^4.2.4",
    "typescript-json-schema": "^0.50.0"
  }
}
